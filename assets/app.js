// App-specific behavior only. Standard menu, dialog, and navigation behavior
// already come from shared/app-shell.js — do not reimplement them here.
//
// Add: loading app-specific data, application state, app-specific
// calculations, and product-specific interactions.

document.addEventListener('DOMContentLoaded', () => {
  // Start the app-specific workflow here.
});
